{
  "name": "piposs-extension",
  "version": "1.0.2",
  "description": "PiPOSS Safari Web Extension",
  "type": "module",
  "scripts": {
    "build": "pnpm run lint:ts && node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "lint:ts": "tsc --project tsconfig.build.json --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "esbuild": "^0.25.10",
    "prettier": "^3.6.2",
    "typescript": "^5.0.0"
  },
  "private": true
}
